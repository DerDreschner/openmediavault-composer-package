{
    "type": "object",
    "properties": {
        "uuid": {
            "type": "string",
            "format": "uuidv4"
        },
        "execution": {
            "type": "string",
            "enum": [
                "hourly",
                "daily",
                "weekly",
                "monthly",
                "yearly"
            ]
        }
    },
    "required": [
        "uuid",
        "execution"
    ]
}