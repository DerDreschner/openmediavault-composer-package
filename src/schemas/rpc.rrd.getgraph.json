{
    "type": "object",
    "properties": {
        "kind": {
            "type": "string"
        },
        "period": {
            "type": "string",
            "enum": [
                "hour",
                "day",
                "week",
                "month",
                "year"
            ]
        }
    },
    "required": [
        "kind",
        "period"
    ]
}